{
  "allowed_files": [
    "profile/airootfs/usr/local/bin/neos-installer-partition.sh"
  ],
  "constraints": [
    "Do not implement new features.",
    "Limit implementation to the specified allowed files.",
    "Resolve option injection vulnerabilities by applying standard option termination (e.g., `--`) before variable arguments."
  ]
}
