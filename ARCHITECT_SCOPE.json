{
  "target_files": [
    "airootfs/usr/local/bin/neos-installer-partition.sh"
  ],
  "instructions": [
    "Implement core Btrfs partitioning logic.",
    "Create standard subvolumes: @, @home, @var, @snapshots.",
    "Include strict block device validation before destructive operations."
  ],
  "constraints": [
    "Limit modifications to the partitioning script.",
    "Do not implement complex RAID or multi-disk logic.",
    "Ensure all variables representing device paths are properly quoted."
  ],
  "dependencies": [
    "None"
  ]
}
