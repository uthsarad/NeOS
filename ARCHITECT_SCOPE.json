{
  "allowed_files": [
    "profile/airootfs/usr/local/bin/neos-autoupdate.sh"
  ],
  "constraints": [
    "Implement dependency validation for snapper and Btrfs root.",
    "Do not implement unrelated features.",
    "Log warnings if dependencies are missing and exit gracefully.",
    "Do not modify the core snapshot creation logic, only add the initial validations."
  ]
}
