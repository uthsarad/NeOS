{
  "authorized_modifications": [
    "Fix Build-Blocking pacman.conf Issue"
  ],
  "constraints": [
    "Ensure changes only affect the build process, preserving installed-system pacman.conf.",
    "Do not implement new features."
  ],
  "target_files": [
    "pacman.conf"
  ]
}
