# Only start open-vm-tools inside VMware. Enabled by default (live session and
# installed systems); anywhere else the daemon would idle uselessly, so skip
# it instead of running a guest agent with no host to talk to.
[Unit]
ConditionVirtualization=vmware
