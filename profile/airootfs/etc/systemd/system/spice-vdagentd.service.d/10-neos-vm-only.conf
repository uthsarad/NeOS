# Only start the SPICE agent inside KVM/QEMU guests, the only platform that can
# provide a SPICE channel. Enabled by default (live session and installed
# systems); anywhere else it would fail against a missing virtio port.
[Unit]
ConditionVirtualization=kvm
