# Only start the VirtualBox guest service inside VirtualBox. Enabled by default
# (live session and installed systems), it would otherwise fail on bare metal
# and every other hypervisor, showing up as a failed unit in `neos-doctor`.
[Unit]
ConditionVirtualization=oracle
