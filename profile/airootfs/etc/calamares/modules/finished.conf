# finished.conf
---
restartNowEnabled: true
restartNowChecked: false
restartNowCommand: "systemctl reboot"

# Palette: [UX] Use a celebratory tone for the completion message.
# Sentinel: [Security] Ensure the reboot command does not leave any sensitive installation artifacts accessible.
