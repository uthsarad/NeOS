# Load common virtualization drivers early for VM installs.
# Keep this list conservative to avoid unnecessary modules on bare metal.
#
# ⚡ Bolt: All modules below are commented out because they are already loaded
# by the initramfs (configured in /etc/mkinitcpio.conf).
# Loading them here causes redundant work for systemd-modules-load.service.

# Virtio (QEMU/KVM)
#virtio
#virtio_pci
#virtio_blk
#virtio_scsi
#virtio_net
#virtio_console
#virtio_balloon

# VMware
#vmw_vsock_vmci_transport

# Hyper-V
#hv_vmbus
#hv_storvsc
#hv_netvsc
#hv_utils

# VirtualBox
#vboxguest
#vboxsf
#vboxvideo
