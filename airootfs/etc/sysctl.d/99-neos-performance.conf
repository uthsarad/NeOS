# ⚡ Bolt: Optimize VM for ZRAM
# Prefer swapping to ZRAM (fast) over discarding page cache (slow to read back)
vm.swappiness = 180
# Disable read-ahead for swap as ZRAM is random access
vm.page-cluster = 0
# Prevent latency spikes under memory pressure
vm.watermark_boost_factor = 0
