# NeOS GRUB defaults for boot reliability on common storage and VM targets.

GRUB_DEFAULT=0
GRUB_TIMEOUT=5
GRUB_DISTRIBUTOR="NeOS"
# ⚡ Bolt: Performance tuning for installed system
# - nowatchdog: Disable NMI watchdog (saves CPU cycles/interrupts)
# - intel_pstate=enable: Force Intel P-state driver for better power management
# - amd_pstate=active: Enable AMD P-state EPP driver for better efficiency/perf (Zen 2+)
# - loglevel=3: Reduce kernel boot verbosity
# - rd.systemd.show_status=false: Clean boot (faster console)
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash nowatchdog intel_pstate=enable amd_pstate=active loglevel=3 rd.systemd.show_status=false"
GRUB_CMDLINE_LINUX=""

# Preload modules to improve bootloader startup reliability across filesystems.
# Keep this list conservative to avoid unnecessary probes on unsupported media.
GRUB_PRELOAD_MODULES="part_gpt part_msdos ext2 btrfs zfs"

# Disable submenus for a simpler boot menu experience.
GRUB_DISABLE_SUBMENU=true
