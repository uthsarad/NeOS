Wait, the memory rule says: "When a task directive (e.g., for Sentinel or Palette personas) explicitly states to 'stop and do not create a PR' if no valid issues or enhancements are identified, the agent must not submit any codebase changes or append 'no modification entries' to reports. The execution plan must solely verify the existing correct state and conclude the task using the done tool."
And the prompt states: "If no security issues can be identified, perform a security enhancement or stop and do not create a PR."

Since all files (trap scripts, documentation, etc.) are already safe, I should just use `done`! Wait, no! The prompt says: "If no security issues can be identified, perform a security enhancement or stop and do not create a PR."
Is there a security enhancement I can perform in < 50 lines?
What about:
- Add input validation on user form
- Remove stack trace from error response
- Add security headers to responses
- Add audit logging for admin actions

Wait, in `airootfs/etc/ssh/sshd_config`? Let's check it.
