[package]
name = "neos-profile-audit"
version = "0.1.0"
edition = "2021"
description = "Rust-based validator for NeOS Archiso profile manifests"
license = "MIT"

[dependencies]

[dev-dependencies]
tempfile = "3.10"
