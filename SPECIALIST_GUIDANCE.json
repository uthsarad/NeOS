{
  "bolt": {
    "focus": "Validation Performance and Build Speed",
    "tasks": [
      "Ensure the Rust `profiledef.sh` parser uses efficient reading mechanisms (e.g. standard file I/O avoiding excessive allocation or backtracking) to keep the CLI fast.",
      "Verify that removing shell execution overhead in `verify_build_profile.sh` slightly improves or keeps CI test suite duration flat."
    ]
  },
  "palette": {
    "focus": "CLI Tool Usability",
    "tasks": [
      "Ensure the error messages emitted by `neos-profile-audit` for missing files, missing `pacman_conf`, or invalid `bootmodes` are clear, context-rich, and actionable for developers (e.g., listing valid options if an invalid bootmode is found)."
    ]
  },
  "sentinel": {
    "focus": "Validation Integrity",
    "tasks": [
      "Review the new Rust parser for logic bypasses (e.g., ensure it correctly handles quotes and spaces like `pacman_conf=\"pacman.conf\"`).",
      "Ensure that `DatabaseOptional` and `DatabaseRequired` verifications, if relocated or touched, continue to accurately reflect the required security posture for the build and installed environments respectively."
    ]
  }
}