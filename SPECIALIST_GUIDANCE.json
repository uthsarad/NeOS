{
  "specialists": {
    "bolt": {
      "focus_area": "Service Boot Performance",
      "tasks": [
        {
          "description": "Verify that the added systemd sandboxing directives do not introduce measurable performance regressions during service startup.",
          "target_file": "airootfs/etc/systemd/system/*.service"
        }
      ]
    },
    "palette": {
      "focus_area": "Developer UX in Service Logs",
      "tasks": [
        {
          "description": "Ensure any failures related to tightened systemd sandboxing (e.g., permission denied) are logged with clear, actionable context for developers.",
          "target_file": "airootfs/etc/systemd/system/*.service"
        }
      ]
    },
    "sentinel": {
      "focus_area": "Privilege Escalation Prevention",
      "tasks": [
        {
          "description": "Audit the updated systemd service units to ensure sandboxing is correctly applied and prevents privilege escalation or unauthorized file system access.",
          "target_file": "airootfs/etc/systemd/system/*.service"
        }
      ]
    }
  }
}