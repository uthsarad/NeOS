{
  "bolt": {
    "focus": "Performance Optimization",
    "instructions": [
      "Review the trap commands in custom bash scripts to ensure they minimize subshell overhead and use native variables efficiently."
    ]
  },
  "palette": {
    "focus": "Developer/Admin UX and Error Messages",
    "instructions": [
      "Ensure the format of logged error messages in custom script traps is clear, uses structural visual cues, and includes actionable steps."
    ]
  },
  "sentinel": {
    "focus": "Security Validation",
    "instructions": [
      "Verify that trap commands safely handle variable expansion without introducing command injection risks or masking exit codes."
    ]
  }
}
