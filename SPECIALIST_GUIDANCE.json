{
  "specialists": {
    "bolt": {
      "focus_area": "Performance Optimization in Error Handling",
      "tasks": [
        {
          "description": "Review the trap commands in custom bash scripts to ensure they minimize subshell overhead and use native variables efficiently.",
          "target_file": "airootfs/usr/local/bin/neos-liveuser-setup"
        },
        {
          "description": "Review the trap commands in custom bash scripts to ensure they minimize subshell overhead and use native variables efficiently.",
          "target_file": "airootfs/usr/local/bin/neos-installer-partition.sh"
        }
      ]
    },
    "palette": {
      "focus_area": "Developer/Admin UX in Error Logs",
      "tasks": [
        {
          "description": "Ensure the format of logged error messages in custom script traps is clear, uses structural visual cues, and includes actionable steps.",
          "target_file": "airootfs/usr/local/bin/neos-liveuser-setup"
        },
        {
          "description": "Ensure the format of logged error messages in custom script traps is clear, uses structural visual cues, and includes actionable steps.",
          "target_file": "airootfs/usr/local/bin/neos-installer-partition.sh"
        }
      ]
    },
    "sentinel": {
      "focus_area": "Security Validation in Shell Scripts",
      "tasks": [
        {
          "description": "Verify that trap commands safely handle variable expansion without introducing command injection risks or masking exit codes.",
          "target_file": "airootfs/usr/local/bin/neos-liveuser-setup"
        },
        {
          "description": "Verify that trap commands safely handle variable expansion without introducing command injection risks or masking exit codes.",
          "target_file": "airootfs/usr/local/bin/neos-installer-partition.sh"
        }
      ]
    }
  }
}