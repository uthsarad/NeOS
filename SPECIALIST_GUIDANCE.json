{
  "bolt": {
    "focus": "Clearing UI performance validation debt",
    "directive": "Execute your pending task to monitor Plasma startup overhead. Ensure that any default LookAndFeel or shortcut configurations in profile/airootfs/etc/xdg/ do not compromise the desktop's initialization speed.",
    "tasks": [
      "profile/airootfs/etc/xdg/*"
    ]
  },
  "palette": {
    "focus": "Clearing UX and logging validation debt",
    "directive": "Complete your pending task regarding journal log clarity for sandboxed services, and validate the accessibility and contrast of Phase 6 Windows-familiar shortcut integrations.",
    "tasks": [
      "profile/airootfs/etc/systemd/system/*.service",
      "profile/airootfs/etc/xdg/*"
    ]
  },
  "sentinel": {
    "focus": "Clearing systemd and desktop security debt",
    "directive": "Aggressively audit the pending systemd sandboxing directives. Ensure neos-autoupdate.service and neos-liveuser-setup.service in profile/airootfs/etc/systemd/system/ have strictly minimal ReadWritePaths and no unnecessary privileges. Additionally, audit newly introduced desktop entries in profile/airootfs/etc/xdg/ for unintended execution paths.",
    "tasks": [
      "profile/airootfs/etc/systemd/system/*.service",
      "profile/airootfs/etc/xdg/*"
    ]
  },
  "note": "All specialists must focus exclusively on resolving pending items in their respective ai/tasks/*.json manifests. Architect is paused."
}