{
  "bolt": {
    "focus": "Performance of error handling",
    "instructions": [
      "Ensure trap commands and error logging minimize subshell overhead.",
      "Prefer native bash variables over external process calls in error paths."
    ]
  },
  "palette": {
    "focus": "UX of terminal output",
    "instructions": [
      "Ensure logged error messages are clear and contain actionable steps for users.",
      "Use structural visual cues in terminal output for critical failures."
    ]
  },
  "sentinel": {
    "focus": "Security of execution paths",
    "instructions": [
      "Verify that trap commands safely handle variable expansion without introducing command injection risks.",
      "Ensure TOCTOU vulnerabilities are not introduced during file creation or logging."
    ]
  }
}
