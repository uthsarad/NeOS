{
  "bolt": {
    "focus": "Optimize CI execution time",
    "tasks": [
      "Ensure security scanning jobs run concurrently where possible to minimize CI delay."
    ]
  },
  "palette": {
    "focus": "No UX changes required",
    "tasks": []
  },
  "sentinel": {
    "focus": "Validate CI Security",
    "tasks": [
      "Ensure the added security scanning tools are configured with appropriately strict rules.",
      "Verify that secrets (if any) are handled securely in the workflow."
    ]
  }
}
