{
  "bolt": {
    "focus": "CI Pipeline Performance",
    "tasks": [
      "Ensure the new pre-build CI test step is efficient and runs quickly before the main build, minimizing any additional CI minute overhead."
    ]
  },
  "palette": {
    "focus": "N/A",
    "tasks": [
      "No UX changes required for this backend CI/Security task."
    ]
  },
  "sentinel": {
    "focus": "Supply Chain Security Validation",
    "tasks": [
      "Verify that the `alci_repo` signature enforcement (`SigLevel = Required DatabaseOptional`) successfully mitigates the supply chain risk identified in the prior `SENTINEL_REPORT.md`.",
      "Ensure no new unsigned repositories are introduced into the build process."
    ]
  }
}