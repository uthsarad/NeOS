{
  "bolt": {
    "focus": "Performance validation of dependency checks",
    "guidance": "Ensure dependency checks avoid slow subshells. Use native bash builtins or efficient external commands."
  },
  "palette": {
    "focus": "UX of warning messages",
    "guidance": "Review the clarity and phrasing of the logged warning messages."
  },
  "sentinel": {
    "focus": "Security of dependency validations",
    "guidance": "Validate that the dependency checks (command -v, findmnt) are robust against path manipulation. Ensure graceful exits do not bypass necessary cleanup."
  }
}
