{
  "Bolt": {
    "focus_area": "Performance Optimization",
    "guidance": "Ensure that the added dependency checks in `airootfs/usr/local/bin/neos-autoupdate.sh` are efficient and do not introduce unnecessary subprocess overhead. Favor native bash builtins where possible."
  },
  "Palette": {
    "focus_area": "UX & Accessibility",
    "guidance": "Ensure any errors logged or surfaced by `airootfs/usr/local/bin/neos-autoupdate.sh` are clear, actionable, and user-friendly. In `README.md` and `docs/HANDBOOK.md`, ensure the updated architecture limitations are formatted clearly, using bulleted lists for readability."
  },
  "Sentinel": {
    "focus_area": "Security Audit",
    "guidance": "Verify that the added dependency checks in `airootfs/usr/local/bin/neos-autoupdate.sh` do not introduce any command injection vulnerabilities or unexpected side effects."
  }
}