{
  "role": "Sentinel",
  "focus_area": "Workflow Action Permissions and Constraints",
  "task": "Verify that granting the `workflows: write` permission in `.github/workflows/jules-auto-merge.yml` is strictly coupled with the existing actor verification logic (`if: github.actor == ...`) to prevent untrusted pull requests from executing unauthorized merges or altering repository configurations.",
  "files": [
    ".github/workflows/jules-auto-merge.yml"
  ]
}