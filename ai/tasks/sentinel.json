{
  "focus_area": "Security Validation",
  "tasks": [
    {
      "description": "Verify that the new auto-merge authorization for NimuthuGanegoda does not bypass required status checks.",
      "target_file": ".github/workflows/jules-auto-merge.yml"
    },
    {
      "description": "Audit the Calamares configuration to ensure no insecure defaults are introduced during the installer implementation.",
      "target_file": "airootfs/etc/calamares/settings.conf"
    }
  ]
}