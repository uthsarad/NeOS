{
  "focus_area": "CI Privileged Execution Review",
  "guidance": "Review the CI workflow changes in `.github/workflows/build-iso.yml` to ensure the privileged execution of the `test` job does not introduce unintended security risks outside the intended scope of pre-build validation.",
  "files_impacted": [
    ".github/workflows/build-iso.yml"
  ]
}