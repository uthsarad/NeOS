{
  "focus_area": "Privileged Execution Review",
  "guidance": "Review the CI workflow (`.github/workflows/build-iso.yml`) changes. Ensure the privileged execution (`--privileged`) of the `test` job running inside the `archlinux:latest` container does not introduce unintended security risks outside the intended scope of pre-build validation.",
  "files_impacted": [
    ".github/workflows/build-iso.yml"
  ]
}
