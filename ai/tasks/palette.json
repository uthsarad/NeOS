{
  "focus_area": "Developer UX and Error Messages",
  "tasks": [
    {
      "description": "Ensure any error messages emitted by the new pacman.conf validation in tests/verify_build_profile.sh are multi-line and provide actionable '💡 How to fix:' guidance.",
      "target_file": "tests/verify_build_profile.sh"
    },
    {
      "description": "Ensure terminal output from tests is clear and reduces developer cognitive load on failure.",
      "target_file": "tests/verify_build_profile.sh"
    }
  ]
}