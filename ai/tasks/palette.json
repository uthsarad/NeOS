{
  "deliverable": "Build Stabilization and CI Guardrails",
  "focus_area": "CI Error Messages",
  "guidance": "Review any output generated by the new ISO size validation step in `.github/workflows/build-iso.yml`. Ensure that if the validation fails, the terminal output clearly communicates human-readable byte values (e.g., MiB via `awk` or `numfmt`) and provides explicit, actionable, multi-line remediation instructions."
}
