{
  "focus_area": "Performance Optimization",
  "tasks": [
    {
      "description": "Ensure the logging mechanism avoids excessive subshell overhead where possible, relying on native variables like $LINENO.",
      "target_file": "airootfs/usr/local/bin/neos-installer-partition.sh"
    },
    {
      "description": "Ensure the logging mechanism avoids excessive subshell overhead where possible, relying on native variables like $LINENO.",
      "target_file": "airootfs/usr/local/bin/neos-liveuser-setup"
    }
  ]
}