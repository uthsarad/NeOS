{
  "focus_area": "Performance Script Optimization",
  "guidance": "Review the test scripts (`tests/verify_mkinitcpio.sh`, `tests/verify_qml_enhancements.sh`) to ensure native bash operations are preferred over subprocess-heavy pipelines (like grep or sed) where applicable, maintaining fast execution.",
  "files_impacted": [
    "tests/verify_mkinitcpio.sh",
    "tests/verify_qml_enhancements.sh"
  ]
}
