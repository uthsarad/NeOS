{
  "focus_area": "Performance Optimization",
  "tasks": [
    {
      "description": "Optimize GPU detection in neos-driver-manager to avoid unnecessary module probing.",
      "target_file": "airootfs/usr/local/bin/neos-driver-manager"
    },
    {
      "description": "Review partitioning script for opportunities to use async discard on Btrfs formatting.",
      "target_file": "airootfs/usr/local/bin/neos-installer-partition.sh"
    },
    {
      "description": "Evaluate if filtering lspci output directly with regex is faster than piping to grep.",
      "target_file": "airootfs/usr/local/bin/neos-driver-manager"
    },
    {
      "description": "Optimize lspci parsing for network controllers to reduce command execution overhead.",
      "target_file": "profile/airootfs/usr/local/bin/neos-driver-manager"
    }
  ]
}
