{
  "task": "Review the newly added Btrfs filesystem check in neos-autoupdate.sh.",
  "context": "The Architect has added a dependency check using findmnt to verify the root filesystem is Btrfs.",
  "action": "Ensure that findmnt is utilized efficiently and doesn't introduce performance regressions before the update runs.",
  "files": [
    "airootfs/usr/local/bin/neos-autoupdate.sh"
  ]
}