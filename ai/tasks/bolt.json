{
  "Bolt": {
    "focus_area": "Performance Optimization",
    "tasks": [
      {
        "description": "Review the newly created `tests/verify_iso_size.sh` script. Replace repeated external subprocess calls (like `grep`) with native bash logic (like substring matching) wherever possible to eliminate fork/exec overhead.",
        "target_file": "tests/verify_iso_size.sh"
      }
    ]
  }
}
