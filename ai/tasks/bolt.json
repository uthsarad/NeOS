{
  "deliverable": "Pre-build CI Validation and Config Fixes",
  "focus_area": "CI/CD Pipeline Optimization",
  "guidance": "Review the new `.github/workflows/build-iso.yml` changes. Ensure file discovery logic utilizes native bash globbing instead of slow subprocesses (like `find` or `awk`) where possible. Ensure the overall CI execution time is not adversely impacted."
}
